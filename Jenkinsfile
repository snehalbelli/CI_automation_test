pipeline{
    agent any
    stages{
        stage("clone repo"){
            steps{
                sh "git --version"
            }
            
        
        }
        stage("build image"){
            steps{
                sh "docker --version"
            }
        }
        stage("push image"){
            steps{
                sh "docker --version"
            }
        }
    }
}